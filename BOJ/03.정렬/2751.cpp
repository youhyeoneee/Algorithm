#include <iostream>

using namespace std;

int main(){
    int N; // 수의 개수 N
    cin >> N;

    int a[N+1] = {0, };// 수열

    a[0] = 0;
    for(int i=1; i<=N; i++)
        cin >> a[i];

    for(int i=1; i<=N)

    return 0;
}